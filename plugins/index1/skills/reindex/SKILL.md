---
description: Rebuild index1 project index
---

Run the following steps:

1. Run `index1 index ./src ./docs --force` to rebuild the index
2. Run `index1 status` to check results
3. Report the document count and chunk count
